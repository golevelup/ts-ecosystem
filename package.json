{
  "name": "ts-ecosystem",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Jesse Carter <jesse.r.carter@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna:publish": "yarn lerna run build && yarn test && yarn lerna publish -m 'chore: publish'",
    "lerna:prerelease": "yarn lerna run build && yarn lerna publish prerelease --preid rc -m 'chore: publish prerelease'"
  },
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "jest": "^24.9.0",
    "lerna": "^3.19.0",
    "lint-staged": "^9.5.0",
    "ts-jest": "^24.3.0",
    "typescript": "^3.7.4"
  }
}
